import { type Dictionary, t } from 'intlayer'

const filterPopoverContent = {
  key: 'browse-filter-popover',
  content: t({
    ja: {
      allLabel: 'すべて'
    }
  })
} satisfies Dictionary

export default filterPopoverContent
